import styled from 'styled-components'

export default function Builder() {
  return <BuilderContainer id="editor"></BuilderContainer>
}

const BuilderContainer = styled.div`
  background-color: #e0e0e0;
`
